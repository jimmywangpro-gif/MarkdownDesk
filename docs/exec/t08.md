# EOR 記錄 — T08 安全強化、拖放與外部連結

- 日期：2026-08-28
- 執行者：Codex（`gpt-5.6-luna`，openai-codex，reasoning `xhigh`）
- 狀態：完成 ✅（agent commits `5349ebc` + corrective `34221b8`，合併 main `fe6c05b`）

## 修改檔案

- `src/dnd.ts` / `src/dnd.test.ts`：`.md` 拖放判定、dirty guard callback、非 Markdown/缺路徑處理。
- `src/previewLinks.ts` / test：只將 `http(s)`/`mailto` 外部連結交給系統 opener；內部 anchor 與不支援協議不導航。
- `src-tauri/tauri.conf.json`：production CSP；修正後 `style-src 'self'`，無 `unsafe-inline`/`unsafe-eval`。
- `src-tauri/capabilities/default.json`：opener 僅允許 `http://*`、`https://*`、`mailto:*`。
- `src-tauri/tauri.conf.test.ts`：CSP invariant regression test。

## 實際驗證

- Agent self-test：T08 初版 `npm test` 46 passed、build、Rust 11 passed；corrective cycle 後未提供完整獨立 RED transcript，記為 `RED UNVERIFIED`。
- Hermes corrective L1–L4：`npm test` 47 passed、`npm run build` 通過、`cargo test --manifest-path src-tauri/Cargo.toml` 11 passed；CSP test 通過。
- 合併後 main L5：前端 56 passed、Rust 13 passed、build、`git diff --check` 通過。

## 重要修正

- Hermes 安全檢查首次發現 `style-src 'unsafe-inline'`，未合併初版；交由 Codex 以 corrective cycle 移除並補 regression test。

## 未驗證

- 實際拖曳檔案至 macOS Tauri webview、系統瀏覽器開啟與 CSP runtime 導航未在 GUI 中執行；純模組、設定與安全 invariant 已驗證。
