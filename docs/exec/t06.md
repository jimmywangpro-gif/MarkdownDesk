# EOR 記錄 — T06 PDF 列印

- 日期：2026-08-28
- 執行者：pi agent（`gpt-5.6-luna`，openai-codex，thinking `xhigh`）
- 狀態：功能模組完成 ✅（agent commit `859024f`，合併 main `21850e8`）

## 修改檔案

- `src/lib/printPdf.ts`：以 `window.print()` 觸發 webview 原生列印；edit-only 時暫切 view，列印後在 `finally` 還原模式。
- `src/lib/printPdf.test.ts`：原生列印呼叫、edit mode 還原、列印 CSS 行為測試。
- `src/print.css`：白底黑字、隱藏 toolbar/editor、預覽全寬、程式碼高亮紙張配色、分頁避免斷裂。
- `src/main.tsx`：引入列印 stylesheet。

## 自我測試（pi 回報 + Hermes 獨立重跑）

- `npm test`：6 files / **42 passed**。
- `npm run build`：成功；僅既有 Vite chunk size advisory（JS gzip 約 158.11KB）。
- `git diff --check`：通過。
- Rust baseline：合併前後 `cargo test` **11 passed**。

## RED / UNVERIFIED

- pi 提供了測試與 GREEN 結果；本次交付未留下可獨立核對的 RED transcript，RED 證據記為 `UNVERIFIED`。
- macOS 真正 native print dialog 與產出 PDF 尚未由 Hermes 實機驗證；jsdom `window.print` mock 不能替代，標記 `UNVERIFIED`。
- Windows/Linux 列印行為待目標平台驗證。

## 驗證與範圍

- 變更未修改 `App.tsx`、render pipeline、fileOps、Rust，符合共享檔整合邊界。
- T09 將統一把 `printPdf` 接到 App toolbar，並在整合後做跨功能回歸。
