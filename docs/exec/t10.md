# EOR — T10 final regression and documentation

- 日期：2026-08-28
- 執行者：pi agent（gpt-5.6-luna, openai-codex, `xhigh`）
- worktree：`/private/tmp/markdowndesk-t10`
- branch：`feat/t10-regression-docs`
- 基線：`ae2874e feat(t09): reproducible cross-platform packaging pipeline`（`main` 與本分支目前同指此 commit）
- 狀態：文件完成；release/package 完整性不宣稱

## Read-first 結果

- 已讀：`docs/SPEC.md`、`.scratch/markdowndesk-mvp/issues/01–10`（共 10 張 ticket）、既有 `docs/exec/t01.md`–`t09.md`、`docs/PITFALLS.md`、T09 的 `docs/packaging.md`，以及現有 frontend/Rust tests、T09 packaging scripts/config/workflow。
- 已確認原始 tickets 實際位於 `.scratch/markdowndesk-mvp/issues/`；沒有以錯誤路徑的空結果補推論。
- 已讀 `docs/exec/t09.md`；它記錄 T09 在 `/tmp/markdowndesk-t09` 的實際 macOS packaging 命令、fallback、DMG bytes 與 Windows/Linux `UNVERIFIED` 邊界。
- 初始 `git status --short --branch`：本分支乾淨於功能檔，但有 harness 產生且未由本票修改的 `?? .pi-sessions/`。

## 文件 RED / 最小變更

1. 建檔前執行缺失文件檢查：`test -f docs/ACCEPTANCE.md` 預期以 exit 1 結束，輸出 `RED: docs/ACCEPTANCE.md is not present yet`。
2. 建檔前第一次 `npm test` 因 worktree 沒有 `node_modules` 以 exit 127 結束：`sh: vitest: command not found`。
3. 依照既有 lockfile 執行 `npm ci`，未修改本票 scope 外的 source、config、package 或 tests；之後重新執行完整驗證。
4. 沒有新增文件檢查 script；沒有為了文件工作修改功能程式碼、既有測試或 workflow。

## 本票修改檔案

- `README.md`：新增使用者安裝、三平台開發/啟動、測試、Tauri native packaging、安裝步驟、限制與未驗證聲明。
- `docs/ACCEPTANCE.md`：逐條列出 SPEC 22 user stories、PASS/PARTIAL/UNVERIFIED、測試路徑、平台狀態與 T09 evidence boundary。
- `docs/exec/t09.md`：由 Hermes 依實際 T09 packaging 執行結果補建的追溯 EOR；不是 pi 的 T10 產出。
- `docs/exec/t10.md`：本 EOR。
- `docs/PITFALLS.md`：只在檔案末尾 append T10 新發現，未重寫既有紀錄。

## Self-test 實際結果

### Frontend

命令：

```sh
npm test && npm run build
```

結果：**通過**。

- Vitest：**11 test files passed；59 tests passed**。
- Vite build：**通過**（Vite 7.3.6，`dist` 產出完成）。
- build 保留一個既有 advisory：minified chunk 大於 500 kB；不是 failure。實際輸出列出的 JS 是 524.82 kB，gzip 160.98 kB。

### Rust

命令：

```sh
cargo test --manifest-path src-tauri/Cargo.toml
```

結果：**通過**。

- `src/lib.rs`：13 passed，0 failed。
- `src/main.rs`：0 tests，通過。
- Doc-tests：0 tests，通過。

### Diff whitespace

命令：

```sh
git diff --check
```

結果：交付前執行並通過，無 whitespace error。

### Packaging contract / manifest check

命令：

```sh
npm run packaging:test
npm run packaging:validate
```

結果：contract test **9 passed, 0 failed**；manifest validation 輸出 `UNVERIFIED: no package manifest found at .../src-tauri/target/packaging/macos/manifest.json` 並正常結束。這是「沒有 manifest」的明確結果，不是 package 成功證據。

## Packaging / platform evidence

- 本票未執行 `npm run package`，因此沒有製造新的套件；T09 已在獨立 worktree 實際執行並產出 macOS DMG，詳見 `docs/exec/t09.md`。
- T09 實測 macOS Darwin arm64 DMG 為 3,778,078 bytes（3.603 MiB），package-file size guard `VERIFIED`；這不是 installed footprint 證據。
- 本票 worktree 沒有保留 ignored `src-tauri/target/packaging/` manifest；manifest 的實際 T09 結果已由 EOR 記錄，沒有把 workflow 設定當作成功 run。
- Windows 10/11 x64：未執行，`UNVERIFIED`。
- Linux x64：未執行，`UNVERIFIED`。
- macOS native DMG GUI smoke、安裝/啟動、拖放、外部瀏覽器、native save/print dialog 未執行。

## 文件缺口

- T09 已有獨立 EOR `docs/exec/t09.md`，記錄實際 package 命令、macOS DMG bytes 與 runner/platform 邊界；Windows/Linux runner、安裝後大小、安裝/啟動 smoke 與簽章政策仍待目標環境補證。
- SPEC story 22 目前只有 `windowState` schema/JSON round-trip，找不到實際捕捉與套用視窗位置/尺寸的 runtime flow；接受矩陣標為 `UNVERIFIED`。
- SPEC story 5 目前有 disabled checkbox rendering golden，但沒有互動 checkbox 回寫 Markdown 的證據；接受矩陣標為 `PARTIAL`。
