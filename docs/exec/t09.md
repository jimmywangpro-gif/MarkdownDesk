# EOR — T09 packaging and App integration

- 日期：2026-08-28
- 執行者：Codex（`gpt-5.6-luna`，openai-codex，reasoning `xhigh`）
- worktree：`/private/tmp/markdowndesk-t09`
- branch：`feat/t09-packaging`
- agent commit：`ae2874e feat(t09): reproducible cross-platform packaging pipeline`
- 合併基線：main 已含 T01–T08；T09 只整合既有 public seams 並新增 packaging pipeline。

## 修改檔案

- `src/App.tsx`：接線 T05 HTML export、T06 native print、T08 drag/drop 與 external-link opener；保留 T03/T04/T07 行為。
- `src/App.integration.test.tsx`：toolbar、drop-open、external-link integration tests。
- `scripts/packaging.mjs`、`scripts/package.mjs`、`scripts/validate-packaging.mjs`、`scripts/packaging-contract.node.mjs`：native-host mapping、dry-run、manifest、byte-size guard 與 macOS hdiutil fallback。
- `package.json`：`package`、`packaging:test`、`packaging:validate` scripts。
- `src-tauri/tauri.conf.json`：明列 `dmg`、`nsis`、`deb`、`appimage` targets，及 macOS minimum version。
- `.github/workflows/package.yml`：macOS/Windows/Linux runner matrix 與 artifact upload。
- `docs/packaging.md`：實際平台限制、size evidence 邊界與 fallback 說明。

## 實際驗證

- `npm test`：11 test files、59 tests passed。
- `npm run build`：通過；Vite 既有 chunk-size advisory，JS 524.82 kB、gzip 160.98 kB。
- `cargo test --manifest-path src-tauri/Cargo.toml`：13 passed、0 failed。
- `npm run packaging:test`：9 passed、0 failed。
- `npm run packaging:validate`：manifest 存在時回報 Tauri target mapping `macos -> dmg`，size guard `VERIFIED`。
- `npm run package`（Hermes 獨立重跑，Darwin arm64）：Tauri `bundle_dmg.sh` 因 headless/Finder helper 失敗；pipeline 確認同一次產出的 fresh `.app` 後，以原生 `hdiutil` 成功建立真實 DMG。
- 實際 DMG（main 最後一次重跑）：`MarkdownDesk_0.1.0_aarch64.dmg`，3,778,010 bytes（3.603 MiB），manifest `sizeStatus: VERIFIED`。T09 首次獨立重跑為 3,778,078 bytes；兩者皆低於 15 MiB 上限。
- `git diff --check`：通過。

## 未驗證與邊界

- Windows NSIS、Linux deb/AppImage：沒有實際 target runner 結果，標示 `UNVERIFIED`；workflow 設定不是成功執行證據。
- 已驗證的是 package-file bytes，不是安裝後 footprint。
- macOS DMG 的 native Finder/安裝/啟動 GUI smoke、HTML save dialog、PDF print dialog、拖放與系統瀏覽器實機操作仍 `UNVERIFIED`。
- `npm run tauri build` 裸命令的 Tauri Finder DMG helper 失敗；正式可重跑入口是 `npm run package`，其 fallback 會拒絕 stale/missing `.app`，不產生假 artifact。

## TDD 證據

Packaging contract 與 App integration tests 已建立並通過；本 EOR 未取得可靠的逐步 RED transcript，故 RED 記為 `UNVERIFIED`，未由後續 GREEN 結果反推。
