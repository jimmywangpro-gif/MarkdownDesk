# EOR 記錄 — T07 主題 + 設定持久化

- 日期：2026-08-28
- 執行者：pi agent（ollama, deepseek-v4-flash:0731-cloud, --thinking max）
- 狀態：完成 ✅（合併 95b7638）

## 修改檔案

- src/theme.css（新建：light/dark CSS custom properties token，data-theme 切換）
- src/lib/settings.ts + settings.test.ts（新建：設定 load/save + Rust command 包裝）
- src/lib/SettingsContext.tsx（新建：Provider + useSettings）
- src/main.tsx（Provider 包裹）
- src/App.tsx（工具列：主題切換/字級控制）
- src/App.css（append：token 引用 + toolbar 樣式）
- src-tauri/src/lib.rs（append：load_settings/save_settings commands + serde structs + settings_tests）
- src/App.settings.test.tsx（新建測試檔）

## 自我測試（pi 回報 + Hermes 獨立重跑）

- npm test：17 passed（4 files）
- npm run build：通過（61.99KB gzipped）
- cargo test：3 passed（暫存目錄 JSON round-trip）

## UNVERIFIED 項目

無。

## 事件

- 解釋性決策（pi 主動標註）：「禁改測試檔」與「元件測試先 RED」衝突 → 採新建測試檔、原測試檔不動。Hermes 判定合理，符合 TDD gate。
- 合併衝突：src/App.css（T02 hljs 樣式 vs T07 token 樣式，皆為 append）→ Hermes 解衝突：兩側全保留（先 T02 後 T07），L5 回歸 31/31 綠。
- package-lock name 變更為先前殘留，非 T07 所改。