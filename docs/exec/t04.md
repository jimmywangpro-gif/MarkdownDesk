# EOR 記錄 — T04 檔案整合垂直切片

- 日期：2026-08-28
- 執行者：Codex agent（ollama, deepseek-v4-flash:0731-cloud, 5.5M tokens）
- 狀態：完成 ✅（部分 commit 658ab02 + Hermes 補全合併 ac044cd/1b94628）

## 修改檔案

- src-tauri/src/commands.rs（新：open/read/save/save_as/recent×3/watch/unwatch + 8 tests）
- src-tauri/src/lib.rs（append：mod commands + dialog plugin + 9 commands 註冊）
- src-tauri/Cargo.toml（tauri-plugin-dialog、notify）
- src/lib/fileOps.ts（新：invoke 包裝 + file-changed listen）
- src/App.tsx（開啟/儲存/另存/最近檔/dirty guard/外部修改 mtime 檢查/Cmd+S/beforeunload）

## 自我測試（Hermes 獨立重跑）

- cargo test：9/9 passed（Codex 斷線前已完成）
- npm test（worktree 基線）：9/9；build 62.81KB gzipped
- 合併進 main 後 L5：31/31 + cargo 11/11

## UNVERIFIED 項目

無（程式碼層面完整；UI 對話框實機互動屬 T09 冒煙範圍）。

## 事件

- **exit 1 於交付回報階段**：Codex 尾端連線中斷（`invalid reasoning value "xhigh"` API 拒絕 + Reconnecting 5 次失敗），但程式碼已寫畢 → 依 capacity 流程 commit 部分成果後檢查，實質交付完整。
- App.tsx 230 行完整實作（dirty guard 用 window.confirm、外部修改以 mtime 比對防自我觸發）。
- 合併衝突 ×2（lib.rs 註冊清單、App.tsx 工具列/imports/state）→ Hermes 融合解法：T04 檔案按鈕 + T07 主題/字級群組並存。
- 首次合併 commit 殘留 1 個衝突標記（build 的 tsc 抓到，vitest 因測試檔編譯跳過未抓到）→ 立即修正重跑 L5 全綠。教訓：合併後必跑 **npm run build**（tsc）而非只跑 vitest。