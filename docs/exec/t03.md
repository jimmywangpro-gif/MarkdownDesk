# EOR 記錄 — T03 分屏、模式切換、同步捲動

- 日期：2026-08-28
- 執行者：Codex agent（`gpt-5.6-luna`，openai-codex，reasoning `xhigh`）
- 狀態：完成 ✅（續作 commit `64995c0`，合併至 main `a9f5580`）

## 修改檔案

- `src/App.tsx`：edit/view/split 三模式、快捷鍵、debounce、區塊錨點同步，以及保留 T04 檔案流程/T07 設定並在編輯時標記 dirty。
- `src/App.css`：workspace/模式樣式，並補上既有深色 highlight `@media` 缺少的結尾大括號。
- `src/lib/renderMarkdown.ts`：新增 `renderMarkdownBlocks` 與 block line mapping，沿用既有 sanitize processor。
- `src/lib/useSyncScroll.ts`：游標所在 source line 對應 preview block。
- `src/App.t03.test.tsx`：8 個模式/debounce/anchor 行為測試。

## 自我測試（Codex 回報 + Hermes 獨立重跑）

- `npm test`：5 files / **39 passed**。
- `npm run build`：成功；CSS 語法警告已消失，僅保留既有 Vite chunk size advisory（JS gzip 約 158.11KB）。
- `cargo test --manifest-path src-tauri/Cargo.toml`：**11 passed, 0 failed**。
- `git diff --check`：通過。
- merge-base delta：僅上述 5 個 T03 允許檔案。

## RED / UNVERIFIED

- `RED UNVERIFIED`：續作接手時既有 T03 測試已存在，但沒有可核對的舊 RED transcript；未捏造 RED 證據。
- Windows/Linux UI 與 packaging 尚未驗證，留給 T09。

## 事件

- 舊 T03 分支祖先為 `95b7638`，不含 T04；Codex 先合併目前 main 再完成單一 App.tsx 整合，保留 T02/T04/T07 功能。
- 發現並修正 `src/App.css` 第二個深色 `@media` 缺少 `}`；獨立 build 確認不再出現 CSS parser warning。
- 交付前已使用指定新模型與 `xhigh`，worktree 乾淨；Hermes 完成 L1–L5 回歸。
