# EOR 記錄 — T05 HTML 匯出

- 日期：2026-08-28
- 執行者：pi（`gpt-5.6-luna`，openai-codex，thinking `xhigh`）
- 狀態：完成 ✅（agent 產出由 Hermes commit `3dd42a1`，合併 main `486268d`）

## 修改檔案

- `src/lib/exportHtml.ts`：以共用 sanitized renderer 產生完整離線 HTML；含 title fallback、內嵌樣式、HTML save seam。
- `src/lib/exportHtml.test.ts`：文件結構、GFM/highlight、安全性、取消/寫入失敗測試。
- `src-tauri/src/lib.rs`：註冊 HTML save dialog 與文字檔寫入 command，含 round-trip test。

## 實際驗證

- Agent self-test：`npm test` 45 passed、`npm run build` 通過、`cargo test --manifest-path src-tauri/Cargo.toml` 13 passed；RED transcript 未提供，記為 `RED UNVERIFIED`。
- Hermes L1–L4：相同結果；build bundle gzip 158.11 kB。
- 合併後 main L5：前端 56 passed、Rust 13 passed、build 通過。

## 未驗證

- 透過 macOS 原生 save dialog 的實際 UI 操作未執行；command seam 與 round-trip 已測試。
