# EOR 記錄 — T02 renderMarkdown 完整管線 + golden tests

- 日期：2026-08-28
- 執行者：pi agent（ollama, deepseek-v4-flash:0731-cloud, --thinking max）
- 狀態：完成 ✅（合併 1514b50）

## 修改檔案

- src/lib/renderMarkdown.ts（重寫：unified 管線 remark-parse → remark-gfm → remark-rehype → rehype-highlight → rehype-sanitize → 自寫 hast stringifier）
- src/lib/renderMarkdown.test.ts（GFM 八類 golden + sanitize 案例）
- src/App.css（append：hljs 高亮樣式 light/dark）
- package.json / package-lock.json（僅 7 個允許套件）

## 自我測試（pi 回報 + Hermes 獨立重跑）

- RED 證據：.scratch/t02-evidence/RED.txt（12 failed / 11 passed）
- GREEN：23/23 passed；build 成功
- bundle 增幅：61.24 → 155.20 KB gzipped（+93.96KB ≤ 300KB 預算）

## UNVERIFIED 項目

無。

## 事件

- sanitize schema 額外放行 code/span className（hljs token 需要）。
- 自寫 hast stringifier 取代 rehype-stringify（scope 未允許該依賴）。
- raw HTML 經 sanitize 後整段移除（比 T01 escape 更嚴格）。
- Hermes 合併後 main L5：31/31（含 T07）。