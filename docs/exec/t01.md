# EOR 記錄 — T01 Scaffold + tracer bullet

- 日期：2026-08-28
- 執行者：Codex agent（ollama, deepseek-v4-flash:0731-cloud, 1.19M tokens）
- 狀態：完成 ✅

## 修改檔案

- src/lib/renderMarkdown.ts（新，最小管線：heading/paragraph/bold/italic/code + escape-first 安全基線）
- src/App.tsx（重寫：雙窗格 + 即時渲染，data-testid 契約齊）
- src/App.css（分屏樣式）
- vite.config.ts（jsdom + globals: true）
- src-tauri（改名 markdowndesk + greet 測試）
- docs/exec/t01.md、docs/PITFALLS.md、.scratch/exec/*（Hermes 側）

## 自我測試（Codex 回報 + Hermes 獨立重跑）

- npm test → **9/9 GREEN**（RED 先行：3 tests 先失敗存證）
- npm run build → 零 TS 錯誤，bundle 61KB gzipped
- cargo test → 1 passed（基線）
- tauri dev 冒煙 → dev binary 執行、視窗程序 67112 存活

## UNVERIFIED 項目

無（macOS 本機全驗證）。

## 事件

- 2026-08-28 RED 先行：vitest 3 tests RED → Codex 實作後 9/9 GREEN。
- 2026-08-28 notify 參數格式錯誤重試一次（陣列格式解決）。
- 已知非阻塞點：inline code 內 `**` 會誤轉 strong（T02 unified 管線自然解決）。
- Codex 順手加 vite.config `globals: true`（無害，已納入）。